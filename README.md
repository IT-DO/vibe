# Фото на память

Приложение-фотобудка для Android и iOS. Планшет ставится на стойку на
мероприятии, гости подходят, фотографируются и сразу забирают напечатанный
снимок.

Печать идёт **напрямую на портативный фотопринтер по IPP** — без системного
диалога печати, из которого гость мог бы уйти в настройки планшета.

Целевой принтер — **Xiaomi Instant / Portable Photo Printer 1S**
(ZPDYJ03HT, в обиходе «S1»): сублимация, 300 dpi, лист 10×15 см,
около 57 секунд на отпечаток.

---

## Главное техническое решение

Для устройства Xiaomi первым делом хочется разбирать проприетарный miIO —
как это делают для пылесосов и лампочек. Здесь это не нужно.

Принтер заявляет поддержку **Mopria, AirPrint и Wi-Fi Direct Print**, а обе
первые технологии — это **IPP** (RFC 8010/8011) поверх HTTP на TCP :631.
Значит, с ним можно говорить документированным стандартным протоколом.

Что это даёт киоску:

- **печать без системного диалога** — обязательное требование для планшета,
  который стоит без присмотра;
- **состояние задания**: видно, когда лист действительно вышел;
- **состояние принтера**: кончилась бумага, открыта крышка, остаток ленты;
- **независимость от модели**: формат документа, размер бумаги и разрешение
  выясняются у принтера в рантайме, поэтому приложение работает и с любым
  другим AirPrint/Mopria-принтером, который окажется на площадке.

Подробности и порядок проверки на живом железе — [`docs/PRINTER.md`](docs/PRINTER.md).

## Что уже сделано

| Слой | Состояние |
|---|---|
| Клиент IPP (кодек, операции, HTTP поверх TCP) | Готов, 77 тестов |
| Кодировщик PWG Raster (запасной формат) | Готов, 29 тестов |
| Очередь печати с повторами и паузой | Готова, 34 теста |
| Поиск принтера (mDNS + шлюз + вручную) | Готов, 20 тестов |
| Геометрия и раскладки отпечатка | Готовы, 80 тестов |
| Конечный автомат сценария гостя | Готов, 53 теста |
| Экраны киоска | Готовы, 75 тестов |
| Связка сценария с камерой, сборкой и очередью | Готова, 14 тестов |
| Слой камеры и переходники к платформе | Готовы, 63 теста |
| Приложение целиком (сквозной проход) | Готово, 13 тестов |
| Админка оператора | Готова, 50 тестов |
| Масштабирование под разрешение экрана | Готово, 33 теста |
| Ретро-шрифты интерфейса и подписи | Готовы, 39 тестов |
| Подбор формата бумаги по ответу принтера | Готов, 16 тестов |
| Проверка, помещается ли раскладка на бумагу | Готова, 15 тестов |
| Разведка принтера по Bluetooth | Готова, 36 тестов |
| Сборка листа на Skia | Написана, подпись под тестами (17) |
| Нативные модули Android | Написаны и подключены к проекту |
| Платформенный проект Android | Настроен, собирается одной командой |
| iOS | **Заморожено.** Проект настроен и компилируется, но не развивается |

**Проверено:** 729 тестов (семь секунд на прогон), чистые `tsc --noEmit` и
`eslint`. Тесты разделены на два проекта: `logic` — чистый TypeScript без
React Native, `ui` — экраны, хук сессии и переходники к платформе с
заглушками нативных модулей.

**Не проверено:** приложение ещё не запускалось на живом планшете с живым
принтером. Логика протокола покрыта тестами против поддельного принтера,
но поведение конкретной прошивки Xiaomi — принимает ли она `image/jpeg`,
как называет носитель 10×15, отдаёт ли `job-state` и `marker-levels` —
проверяется только на железе. Порядок проверки расписан в
[`docs/PRINTER.md`](docs/PRINTER.md#что-нужно-проверить-на-живом-принтере).

## Запуск

### Проверка логики — работает прямо сейчас

```bash
npm install
npm run verify        # tsc --noEmit + eslint + jest
```

Ни планшета, ни принтера для этого не нужно: весь протокол печати,
геометрия, очередь и сценарий не зависят от React Native и гоняются на Node.

### Сборка приложения

Платформенные проекты лежат в репозитории и настроены: нативные модули
подключены к целям сборки, разрешения прописаны. Генерировать шаблон не нужно.

**APK без установки инструментов** — раздел **Releases**, релиз
`android-latest`. Прямая ссылка на `.apk`, обновляется каждой успешной
сборкой; открывается прямо в браузере телефона.

**APK на своей машине** (нужны JDK 17, Android SDK 35, NDK 26.1.10909125):

```bash
npm install
cd android && ./gradlew assembleRelease -PreactNativeArchitectures=arm64-v8a
# android/app/build/outputs/apk/release/app-release.apk
```

**iOS заморожен.** Проект под iPad настроен и проверенно компилируется, но
развитие сосредоточено на Android: там нет ежегодной платы, сертификатов и
ограничений на установку, а киоск-режим работает по-настоящему. Файлы
оставлены в репозитории — ветку можно разморозить, ничего не восстанавливая.
Сборка под iOS убрана из автоматических прогонов и запускается вручную:
Actions → «Сборка» → Run workflow → галка «Собрать также под iOS».

Подробно — включая подпись релиза и разбор ошибок сборки —
в [`docs/BUILD.md`](docs/BUILD.md).

Дальше — [`docs/KIOSK-SETUP.md`](docs/KIOSK-SETUP.md): как превратить планшет
в киоск, из которого нельзя выйти.

## Сценарий гостя

```
Заставка (зеркало) → Выбор формата → Приготовьтесь → 3·2·1 → Снимок
                                                                 ↓
Заставка ← Благодарность ← Печать ← Просмотр («Печатать» / «Переснять»)
```

Есть и второй путь: кнопка **«Выбрать готовое фото»** на заставке открывает
галерею и печатает уже имеющийся снимок, минуя съёмку.

Из каждого шага есть выход по таймауту: гость может уйти в любой момент, и
будка освободится сама. Из просмотра снимок при бездействии **уходит в печать
сам** — лишний отпечаток дешевле, чем занятая будка и очередь за спиной.

Печать идёт в фоне: экран освобождается сразу после постановки задания, а не
через 57 секунд.

Разбор каждого экрана и обоснование решений —
[`docs/UI-SPEC.md`](docs/UI-SPEC.md).
Кликабельный прототип, который проходит сценарий целиком, —
[`docs/prototype.html`](docs/prototype.html): откройте файл в браузере.

## Форматы отпечатка

| Формат | Кадров | Что получает гость |
|---|---|---|
| Одно фото | 1 | Снимок во весь лист 10×15 |
| **Полоска на двоих** | 3 | Лист печатается двумя одинаковыми половинами с пунктиром: гости разрывают его и забирают по полосе |
| Четыре кадра | 4 | Сетка 2×2 |
| Полароид | 1 | Кадр с широким белым полем под подпись |
| Два кадра | 2 | «До» и «после» |

## Документация

| Файл | О чём |
|---|---|
| [`docs/UI-SPEC.md`](docs/UI-SPEC.md) | Интерфейс: каждый экран и почему он такой |
| [`docs/prototype.html`](docs/prototype.html) | Кликабельный прототип экрана гостя |
| [`docs/PRINTER.md`](docs/PRINTER.md) | Протокол, топологии сети, проверка на железе, диагностика |
| [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) | Устройство кода и почему логика отделена от React Native |
| [`docs/BUILD.md`](docs/BUILD.md) | Сборка APK и приложения для iPad, подпись, ошибки сборки |
| [`docs/KIOSK-SETUP.md`](docs/KIOSK-SETUP.md) | Подготовка планшета и физическая установка |
| [`docs/EVENT-CHECKLIST.md`](docs/EVENT-CHECKLIST.md) | Чек-лист организатора и типичные неприятности |

## Приватность

Камера снимает чужих людей, поэтому планшет не превращается в архив лиц:
исходные кадры удаляются сразу после сборки листа, напечатанный лист — после
печати, всё остальное — автоматически через сутки. В админке есть кнопка
«Стереть все снимки» для конца мероприятия. Сохранение копий выключено по
умолчанию.
