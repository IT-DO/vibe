/**
 * Тарифы. Одно место, где живут цены.
 *
 * Цены подобраны так, чтобы средний чек окупал себестоимость генерации
 * с большим запасом. Одна генерация обходится примерно в 2-5 рублей
 * (зависит от модели), продаём мы её за 10-19 рублей. Считай сам,
 * когда будешь менять модель или цены.
 *
 * Почему пакеты кредитов, а не подписка: подписку человек боится
 * ("а вдруг забуду отменить"), пакет - это разовая покупка,
 * решение принимается легче. Подписку добавишь, когда будут
 * повторные покупки.
 */

export type Plan = {
  id: string;
  title: string;
  credits: number;
  priceRub: string; // строкой, чтобы не ловить проблемы с плавающей точкой
  perCard: string;
  highlight?: boolean;
  note?: string;
};

export const PLANS: Plan[] = [
  {
    id: "start",
    title: "Старт",
    credits: 30,
    priceRub: "590.00",
    perCard: "≈20 ₽ за карточку",
    note: "Попробовать на одной поставке",
  },
  {
    id: "seller",
    title: "Селлер",
    credits: 150,
    priceRub: "1990.00",
    perCard: "≈13 ₽ за карточку",
    highlight: true,
    note: "Обычный месяц активного продавца",
  },
  {
    id: "agency",
    title: "Агентство",
    credits: 600,
    priceRub: "5900.00",
    perCard: "≈10 ₽ за карточку",
    note: "Если ведёшь чужие магазины",
  },
];

export function findPlan(id: string): Plan | undefined {
  return PLANS.find((p) => p.id === id);
}
