/**
 * Ассеты приложения.
 *
 * Шрифты подписей и заголовков встроены в приложение, а не берутся из
 * системы: на разных прошивках Android набор системных шрифтов отличается,
 * и отпечаток на одном планшете вышел бы не таким, как на другом.
 */
module.exports = {
  project: {
    android: {},
  },
  assets: ['./assets/fonts'],
};
